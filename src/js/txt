// Importar la clase Usuario utilizando la sintaxis de importación
import { Usuario } from './usuario.js';

function RegistroUsuario() {
  // Obtener los valores de los inputs
  var nombre = document.getElementById('inputNombre').value;
  var apellido = document.getElementById('inputApellido').value;
  var email = document.getElementById('input_Email').value;
  var contraseña = document.getElementById('inputContraseña').value;
  var confirmaContraseña = document.getElementById('inputConfirmarContraseña').value;
  var telefono = document.getElementById('inputTelefono').value;
  var pais = document.getElementById('inputPais').value;

  function validarCorreo(correo) {
    var regexCorreo = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if (regexCorreo.test(correo)) {
      return correo; // el correo es válido
    } else {
      return "correo inválido"; // el correo no es válido
    }
  }

  // Llamado a la función de validar el correo electrónico
  var emailConfirmado = validarCorreo(email);

  // VALIDANDO LA CONTRASEÑA
  if (contraseña === confirmaContraseña) {
    console.log("La contraseña coincide");
  } else {
    console.log("La contraseña no coincide");
  }

  // Crear una instancia de la clase Usuario
  // MOSTRANDO LOS DATOS INGRESADOS
}
var usuario = new Usuario(nombre, apellido, emailConfirmado, contraseña, telefono, pais, Genero);
console.log(usuario.nombre, usuario.apellido, usuario.email);
console.log(usuario.email);
console.log(usuario.contraseña, usuario.telefono, usuario.telefono);

